[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = [
  "curl -LsSf https://astral.sh/uv/install.sh | sh",
  "$HOME/.local/bin/uv sync --no-dev"
]

[start]
cmd = "$HOME/.local/bin/uv run gunicorn config.wsgi:application --bind 0.0.0.0:$PORT --workers 2"
