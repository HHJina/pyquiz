[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = [
  "curl -LsSf https://astral.sh/uv/install.sh | sh",
  "source $HOME/.cargo/env",
  "uv sync --no-dev"
]

[start]
cmd = "uv run gunicorn config.wsgi:application --bind 0.0.0.0:$PORT --workers 2"
