[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python manage.py migrate --noinput && python manage.py seed_questions && gunicorn config.wsgi:application --bind 0.0.0.0:$PORT --workers 2"
